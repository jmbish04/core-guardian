name: Fix pnpm Lockfile

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Cloudflare Workers Builds Autoconfig"]
    types:
      - completed

permissions:
  contents: write

jobs:
  sync-lockfile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          # setup-node@v5 defaults package-manager-cache:true, which runs pnpm
          # (from the "packageManager" field) during THIS step — before the
          # Corepack steps below install it — and fails. Corepack handles pnpm.
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Install pnpm
        run: corepack prepare pnpm@latest --activate

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Run Lockfile Fix Script
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 .github/scripts/fix_lockfile.py
