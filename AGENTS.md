{"findings": [{"body": "AGENTS.md is missing explicit references to several `.agent/rules/` files that exist in the repository (`repository-init`, `meta-maintenance`, `dependency-maintenance`, `data-utilities`, `import-paths`, `docstrings`, `frontend-error-handling`, `architecture`, `startup`). The current file only references a subset inline.", "code_location": "AGENTS.md", "confidence_score": 0.95, "priority": 1, "title": "Incomplete rule references"}, {"body": "The file does not mention the project's formatter/linter choice (oxlint/oxfmt via `.oxlintrc.jsonc` / `.oxfmtrc.jsonc`) or Prettier configuration, even though `package.json` scripts rely on them. It also omits the pnpm workspace and Corepack conventions.", "code_location": "AGENTS.md", "confidence_score": 0.92, "priority": 1, "title": "Missing tooling conventions"}, {"body": "Several concrete codebase patterns are absent: D1 migration workflow (`drizzle-kit generate` + `wrangler d1 migrations apply`), the `src/_worker.ts` SSR handler contract, the `client:only=\"react\"` rule for Agents SDK islands, and the isomorphic data toolkit import surfaces (`@/lib/data`, `@/backend/utils/data`, `@/shared/data-utils`).", "code_location": "AGENTS.md", "confidence_score": 0.94, "priority": 1, "title": "Missing runtime and migration conventions"}, {"body": "The existing docstring directive is strong but could be more actionable (file-level JSDoc, exported symbol JSDoc, parameter/returns/throws/example tags). It should also cross-reference `.agent/rules/docstrings.md`.", "code_location": "AGENTS.md", "confidence_score": 0.85, "priority": 2, "title": "Docstring directive can be strengthened"}, {"body": "The file mixes high-level directives with a long reference implementation section. The reference implementation is valuable but should be clearly separated and updated to mention all bound agents and showcase pages visible in the file tree.", "code_location": "AGENTS.md", "confidence_score": 0.8, "priority": 2, "title": "Structure could be clearer"}, {"body": "No guidance is given for MCP verification cadence, dependency refresh ownership, or how to handle stale `worker-configuration.d.ts` / lockfiles. The README covers some of this and should be reflected in AGENTS.md.", "code_location": "AGENTS.md", "confidence_score": 0.88, "priority": 1, "title": "Missing maintenance ownership guidance"}], "overall_confidence_score": 0.9, "overall_correctness":  "patch is correct", "overall_explanation": "The existing AGENTS.md captures many critical directives but is incomplete with respect to the full `.agent/rules/` corpus, the oxlint/oxfmt toolchain, D1 migration workflow, and several concrete runtime patterns visible in the codebase. The patch adds these while preserving the existing structure and tone, and keeps the file under the 12,000-character per-file guideline mentioned in the rules."}